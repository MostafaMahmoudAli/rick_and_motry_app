import 'package:flutter/material.dart';

class CharacterGridViewItem extends StatelessWidget {
  const CharacterGridViewItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
